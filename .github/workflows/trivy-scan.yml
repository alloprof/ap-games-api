name: Security Scans
permissions:
      contents: read
      pull-requests: write
on:
  pull_request:
jobs:
  trivy-vulnerability-scan:
    name: Trivy Vulnerability Scan
    if: github.base_ref == github.event.repository.default_branch
    uses: alloprof/.github/.github/workflows/trivy-scan.yml@v1.1